name: alatpay_sdk
description: A Flutter SDK for Alat Pay integration.
version: 1.0.2+3

homepage: https://github.com/GreatGodson/alatpay_sdk
repository: https://github.com/GreatGodson/alatpay_sdk
issue_tracker: https://github.com/GreatGodson/alatpay_sdk/issues
license: MIT

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  # Core UI and network libraries
  cupertino_icons: ^1.0.8
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/banks/